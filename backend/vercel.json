{
  "version": 3,
  "builds": [
    {
      "src": "tinder2/backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/*",  // Match all incoming URLs
      "dest": "/"   // Don't rewrite URLs (optional, might be needed for some frameworks)
    }
  ]
}
